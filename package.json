{
  "name": "miro-game-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "deploy": "npm run build && wrangler pages deploy dist --project-name miro-portal"
  },
  "devDependencies": {
    "vite": "^8.1.0",
    "wrangler": "^4.105.0"
  }
}
